language: objective-c
branches:
  only:
    - master
script:
  - xctool -workspace Redmadrobot.workspace -scheme Redmadrobot -sdk iphonesimulator -configuration Release ONLY_ACTIVE_ARCH=NO
  - xctool test -workspace Redmadrobot.workspace -scheme RedmadrobotTests -sdk iphonesimulator -configuration Release ONLY_ACTIVE_ARCH=NO
